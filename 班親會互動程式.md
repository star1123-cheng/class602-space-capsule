# Role & Goal
你是一位精通前端視覺互動與 Canvas/物理動效的前端資深工程師。請為我撰寫一個「單一 HTML 檔案（包含 HTML5 + CSS + JavaScript）」的無伺服器互動 Web App，主題為「國小五升六班親會：航向六年級的時空補給站（Antigravity 飄浮信箱）」。

# Visual Concept & Antigravity Effect
1. 背景：深色漸層星空／深藍夜空（Cosmic/Midnight Blue），帶有微小的閃爍星點。
2. 反重力物理引擎（Antigravity）：
   - 畫面上有多個緩慢飄浮、互不重疊且具有微弱彈性碰撞的「無重力留言膠囊」（外觀為半透明玻璃擬物微光氣泡或信封圖標）。
   - 氣泡在畫布中自由無重力漂浮（可使用 Matter.js CDN 或原生 Canvas 輕量物理公式：極低重力、微小阻尼、微幅隨機飄移）。
   - 滑鼠懸停或觸控時，氣泡會產生微小的斥力或發光放大效果。

# Core Features & User Journey

## Mode 1: 家長留言端（親師會現場使用）
1. 畫面中央下方設有醒目的半透明毛玻璃按鈕：「寫下給孩子的航行補給」。
2. 點擊後彈出優雅的 Modal 彈窗表單：
   - 學生座號/姓名（下拉式選單或文字輸入）。
   - 快速靈感標籤（點擊可自動帶入文字片段，如「這五年我看見你的成長是...」、「面對六年級，我相信你...」、「爸爸媽媽永遠是你的後盾」）。
   - 留言內容文字框（支援多行輸入）。
   - 署名（如：愛你的媽媽）。
3. 送出動畫：點擊「封存並發射」，表單關閉，畫面中央誕生一顆新的專屬光芒氣泡，以慢動作升空並融入飄浮的無重力星空中。

## Mode 2: 學生解鎖端（週一晨光時間學生查閱）
1. 畫面右上角設有「學生登入解鎖」按鈕。
2. 點擊後跳出簡易密碼驗證（輸入座號 + 預設預留驗證碼）。
3. 解鎖後動效：
   - 全場其他飄浮氣泡稍微淡出並退至背景。
   - 該學生的專屬氣泡放大飄移至螢幕正中央，伴隨光暈與拆封動效展開為「精緻信紙卡片」。
   - 清楚呈現家長留下的文字與署名。
4. 雙向回饋機制：
   - 信紙下方提供「回覆爸爸媽媽一句話」的輸入框與送出按鈕。

# Technical Constraints & Implementation Details
1. 格式：必須為完整、可獨立執行的「單一 index.html 檔案」，包含完整的 CSS reset 與 JS 邏輯。
2. 函式庫支援：可透過 CDN 引入 Tailwind CSS（排版）、Lucide Icons（圖標）、Matter.js 或 Canvas-confetti（慶祝粒子），但優先保持流暢與輕量。
3. 資料暫存（Mockup）：預設使用瀏覽器 `localStorage` 進行讀寫示範，並在 JS 內預留明確的 Google Apps Script (Web App API) `fetch()` 函式接點，方便未來無縫串接 Google 試算表。
4. RWD 響應式：必須完美支援平板（iPad）與手機直橫式螢幕觸控操作。
5. UI 風格：溫馨、現代、科技感與童趣兼具，文字排版易讀性高（字體清晰、對比度足夠）。